{
  "name": "@pharma-chain/contracts",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "node": "hardhat node",
    "deploy:local": "hardhat run ../scripts/deploy-local.ts --network localhost",
    "deploy:sepolia": "hardhat run ../scripts/deploy-sepolia.ts --network sepolia",
    "grant-roles": "hardhat run ../scripts/grant-roles.ts",
    "generate-frontend-config": "hardhat run ../scripts/generate-frontend-config.ts"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.0",
    "hardhat": "^2.22.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.4.0"
  }
}
